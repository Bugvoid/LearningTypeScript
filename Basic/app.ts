class produto {
  nome: string;
  preco: string;
  constructor(_nome: string, _preco: string) {
    this.nome;
    this.preco;
  }

  getDados() {
    return "Nome do produto: " + this.nome + "preco: " + this.preco;
  }
}
